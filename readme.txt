Dependencies & How To install Required Pacakages:

1) Libraries List Provided In "requirements.txt"
    Run = python3 -m pip install -r requirements.txt

2) Import Db into SQL
    mysql -u root -p root
    create database primes
    mysql -u root -p primes <  primes.sql

EXECECUTION: 

1) Run sever.py

2) Run Client.py
